<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">CourierApp</a>
      <div class="nav-collapse collapse">
      <% if user_signed_in? %>
        <ul class="nav pull-right">
        <li class="dropdown">
          <a href="#" role="button" class="dropdown-toggle" data-toggle="dropdown"> <i class="icon-user"></i> <%= current_user.email %> <i class="caret"></i>
          </a>
          <ul class="dropdown-menu">
            <li>
              <a tabindex="-1" href="#">Profile</a>
            </li>
            <li class="divider"></li>
            <li>
              <li><%= link_to t('sign_out'), destroy_user_session_path, :method=>'delete' %></li>
            </li>
          </ul>
        </li>
        </ul>
        <% else %>
        <ul class="nav pull-right">
	  <li><%= link_to t('sign_in'), new_user_session_path %></li>
	  <li><%= link_to t('register'), new_user_registration_path %></li>
	</ul>
	<% end %>
        <ul class="nav">
        <% model_list.each do |model| %>
          <!-- model == controller.controller_name -->
          <!-- ('/'+model) ====> https://github.com/plataformatec/devise/issues/471 -->
          <li class= "<%= current_page?(controller: ('/'+model).to_sym) ? 'active' : nil %>" >
           <%= link_to model, controller: ('/'+model) %>
          </li>
        <% end %>
        </ul>
      </div>
      <!--/.nav-collapse -->
    </div>
  </div>
</div> 

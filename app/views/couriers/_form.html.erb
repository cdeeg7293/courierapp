<%= form_for(@courier) do |f| %>
  <% if @courier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courier.errors.count, "error") %> prohibited this courier from being saved:</h2>

      <ul>
      <% @courier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<h3> Personal info</h3>
<div id="pers">
  <%= f.fields_for :client do |cl| %>
    <% render "/shared/client_fields", f: cl %>
  <% end %>
</div>

<h3> Address </h3>
<div id="address">
  <%= f.fields_for :address do |addr| %>
    <% render "/shared/address_fields", :f => addr %>
  <% end %>
</div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
